- job:
    name: iam_jjb_auto_<appndid>
    project-type: freestyle
    defaults: global
    description: 'Build and Deploy of petclinc via JJB -Branch Arunesh - Demo'
    block-downstream: false
    concurrent: true
    node: master
    properties:
       - github:
          url: https://github.com/devopsbasservice/springpetclinic
    scm:
       - git:
          url: https://github.com/devopsbasservice/springpetclinic
          branches:
            - arunesh
          git-config-name: Arunesh
          git-config-email: arunesh@gmail.com
          extensions:
            per-build-tag: true
    triggers:
       - pollscm:
          cron: "* * * * *"
    builders:
       - maven-target:
            maven-version: Maven3
            pom: pom.xml
            goals: clean install #sonar:sonar -Dsonar.host.url=http://54.197.152.39:9001/
          #  shell: ssh -oStrictHostKeyChecking=no -i /usr/share/master.pem ubuntu@52.90.150.100 "ifconfig"
          #  shell: cd /var/jenkins/workspace/spring_petclinic_jjb_<appndid>/target/
          #  shell: ssh -oStrictHostKeyChecking=no -i /usr/share/master.pem ubuntu@52.90.150.100 "/usr/share/tomcat7/bin/shutdown.sh"
          #  shell: sleep 10
          #  shell: ssh -oStrictHostKeyChecking=no -i /usr/share/master.pem ubuntu@52.90.150.100 "rm -rf /var/lib/tomcat7/webapps/Locan*"
          #  shell: scp -rp -oStrictHostKeyChecking=no -i /usr/share/master.pem "/var/jenkins/workspace/spring_petclinic_jjb_<appndid>/target/petclinic.war" "ubuntu@52.90.150.100:/var/lib/tomcat7/webapps/"
          #  shell: ssh -oStrictHostKeyChecking=no -i /usr/share/master.pem ubuntu@52.90.150.100 "/usr/share/tomcat7/bin/startup.sh"

    publishers:
       - logstash:
          max-lines: -1
          fail-build: false
    wrappers:
       - mask-passwords
       - logstash:
           use-redis: false

- docker_image:
    name: devopsbasservice/basejenkins_maven_git_java
